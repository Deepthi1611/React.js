

function App() {
  // adding variable in react
  //variable are used using curly braces
  // we use evaluated expression(final outcome) of javascript inside the jsx, not inside the jsx
  const userName  = 'react'

  return (
    <h1>Hello {userName}</h1>
  );
}

export default App;
